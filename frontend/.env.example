# Supabase JS client — used for Auth (Google OAuth, JWT validation)
# This is the HTTPS project URL, NOT the postgres connection string
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Backend API URL
BACKEND_URL=http://localhost:4000
